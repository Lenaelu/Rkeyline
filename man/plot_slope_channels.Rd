% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_slope_channels.R
\name{plot_slope_channels}
\alias{plot_slope_channels}
\title{Terrain Analysis with Stream Network Visualization}
\usage{
plot_slope_channels(
  dtm,
  metrics,
  contour_interval = 10,
  stream_threshold = 1000,
  hillshade_angle = 45,
  hillshade_direction = 270,
  contour_color = "darkorange4",
  contour_width = 1,
  label_size = 0.8,
  slope_alpha = 0.6,
  stream_color = "blue",
  stream_width = 1,
  main_title = "Slope with contours and streams"
)
}
\arguments{
\item{dtm}{SpatRaster. dtm raster object from the terra package.}

\item{metrics}{Optional. A list object returned from \code{calc_geomorph_metrics()}.
If NULL, the function will calculate metrics internally. Default is NULL.}

\item{contour_interval}{Numeric. Elevation interval for contour lines. Default is 10.}

\item{stream_threshold}{Numeric. Threshold value for stream extraction (number of cells).
Higher values result in fewer streams. Default is 1000.}

\item{hillshade_angle}{Numeric. Sun elevation angle for hillshade calculation (0-90 degrees).
Default is 45.}

\item{hillshade_direction}{Numeric. Sun azimuth direction for hillshade (0-360 degrees).
Default is 270.}

\item{contour_color}{Character. Color for contour lines. Default is "darkorange4".}

\item{contour_width}{Numeric. Line width for contours. Default is 1.}

\item{label_size}{Numeric. Size of contour labels. Default is 0.8.}

\item{slope_alpha}{Numeric. Transparency of slope overlay (0-1). Default is 0.6.}

\item{stream_color}{Character. Color for stream network. Default is "blue".}

\item{stream_width}{Numeric. Line width for streams. Default is 1.}

\item{main_title}{Character. Plot title. Default is "Slope with contours and streams".}
}
\value{
A named list containing:
\itemize{
\item \code{dtm}: Original dtm as SpatRaster
\item \code{slope}: Slope in degrees as SpatRaster
\item \code{aspect}: Aspect in radians as SpatRaster
\item \code{hillshade}: Hillshade as SpatRaster
\item \code{contours}: Contour lines as SpatVector
\item \code{streams}: Stream network as SpatRaster
\item \code{flow_acc}: Flow accumulation as SpatRaster
}
}
\description{
Creates a visualization of terrain analysis including slope, contours, and
stream network. This function uses pre-calculated geomorphology metrics from
\code{calc_geomorph_metrics()}.
}
\details{
This function creates a comprehensive terrain visualization by:
\enumerate{
\item Using pre-calculated geomorphology metrics (slope, aspect, hillshade, contours, streams)
\item Creating a multi-layer visualization with hillshade, slope, contours, and streams
}
}
\note{
Requires the following packages: \code{terra}, \code{whitebox}, \code{sf}
}
\examples{
\dontrun{
# Load dtm
dtm <- rast("path/to/dtm.tif")

# Calculate metrics once
metrics <- calc_geomorph_metrics(dtm, stream_threshold = 1000)

# Use metrics for plotting
result <- plot_slope_channels(dtm, metrics = metrics)

# Custom parameters
result <- plot_slope_channels(
  dtm = dtm,
  metrics = metrics,
  contour_interval = 20,
  stream_color = "darkblue"
)

# Access individual results
plot(result$slope)
plot(result$streams)
}

}
